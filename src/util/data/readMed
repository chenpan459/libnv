“数据结构”(Data Structures)是计算机科学中的一个核心概念，指的是数据元素的组织方式。数据结构提供了数据存储、管理和访问数据的机制，它们是编写高效算法的基础。以下是一些常见的数据结构类型:

1. **数组(Array)**:
   - 一种线性数据结构，用于存储相同类型的元素，元素通过索引访问。

2. **链表(Linked List)**:
   - 由节点组成的线性数据结构，每个节点包含数据部分和指向下一个节点的指针。

3. **栈(Stack)**:
   - 遵循后进先出(LIFO)原则的线性数据结构。

4. **队列(Queue)**:
   - 遵循先进先出(FIFO)原则的线性数据结构。

5. **哈希表(Hash Table)**:
   - 通过哈希函数将键(Key)映射到表中一个位置以便快速访问数据。

6. **树(Tree)**:
   - 由节点组成的层次结构，每个节点有零个或多个子节点。

7. **二叉树(Binary Tree)**:
   - 树的每个节点最多有两个子节点(通常称为左子节点和右子节点)。

8. **二叉搜索树(Binary Search Tree, BST)**:
   - 一种特殊的二叉树，其中每个节点的左子树只包含键值小于节点键值的节点，右子树只包含键值大于节点键值的节点。

9. **图(Graph)**:
   - 由顶点(Vertex)和边(Edge)组成的数据结构，用于表示实体之间的复杂关系。

10. **堆(Heap)**:
    - 一种特殊的树形数据结构，满足堆性质:即子节点的键值总是不大于(或不小于)其父节点的键值。

11. **图的树形结构(Tree of Graphs)**:
    - 图的树形结构是一种将图分解为树形结构的方法，常用于图的遍历和搜索。

数据结构的选择取决于特定应用的需求，包括数据的大小、访问模式、更新频率等因素。不同的数据结构在时间复杂度和空间复杂度上有不同的权衡。
